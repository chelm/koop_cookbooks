{
    "server": {
        "port": 80
    },
    "processes":2,
    "logfile": "<%= node[:koop][:logfile] %>",
    "data_dir": "<%= node[:koop][:data_dir] %>",
    "s3": {
      "bucket": "<%= node[:koop][:s3][:bucket] %>"
    },
    "db": {
      "conn": "<%= node[:koop][:db][:conn] %>"
    }
}
