#!/bin/sh
inactive=`/usr/bin/curl -s http://127.0.0.1/agolworkers | /usr/bin/jq '.inactive'`
/usr/local/bin/aws cloudwatch put-metric-data --namespace "<%= node[:koop][:alarm][:namespace] %>" --metric-name "<%= node[:koop][:alarm][:name] %>" --value $inactive
